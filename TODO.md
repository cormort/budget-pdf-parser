# 待處理項目與建議處理方式

紀錄目前已知但尚未處理的解析限制。每項附**成因、建議做法、風險、驗收方式**。
處理任何一項前，建議先叫用 `/rule-fix-loop`：先重現真實失敗案例 → 分類 → 動程式前先量化討論規則 → 拋棄式腳本驗證 → 最小改動 → 再驗證。

基準資料：`examples/moa-fund-115.pdf`＝115 年度農業特別收入基金預算案（現行解析 845 列，含補出的一級母列）。

---

## B. 一句列多科目＋合共（150 列，21%）〔優先度：中〕

**現象**
```
郵電費－編列3,225千元、電話費4,800千元及數據通信費3,386千元，合共11,411千元。
材料及用品費－編列辦公(事務)用品300千元及其他用品消耗750千元，合共1,050千元。
```
一列裡列了多個三級科目、各自帶金額，最後一個「合共」是小計。目前解析器**一列只給一個科目、一個金額**（金額取「合共」，取值是對的），所以個別三級科目的金額沒被拆出來。

**影響**
- 主表無法呈現各三級科目的金額。
- **目前對帳僅存 8 筆差異的主要來源**。已逐筆查證原始 PDF：明細側的
  `專業服務費` 各計畫分列齊全（672,517＋322,665＝995,182 千元，與明細值吻合），
  說明版為 904,242 千元，缺口 90,940 千元確實在說明版的這類句型上。

**建議做法**
1. 偵測條件：`description` 中可比對到科目表的「科目名＋N千元」配對 **≥2 個**，且句中有「合共N千元」。
2. 拆成多列子項，每個科目一列；原「合共」列標記 `isSubtotal: true`。
3. **安全閥（關鍵）**：只有在「子項金額加總 === 合共金額」時才拆；對不上就**維持原狀不拆**。這條讓改動自我驗證，不確定就不動。
4. 對帳彙總只計入 `!isSubtotal` 的列，避免小計與子項重複相加。

**風險**
主表列數、匯出內容、對帳彙總三處都會受影響；重複計算是主要風險，靠步驟 3、4 控制。

**驗收**
- 拆列前後，各基金／二級科目的金額總和**必須不變**。
- 對帳 tab 的「金額不一致」筆數應下降，且不得出現新的大額差異。

---

## C. 無科目名的用途活動句（182 列，26%）〔優先度：低／建議維持現狀〕

**現象**
```
提升飼料安全品質及強化風險管理等40,000千元。
建構韌性養豬產業及導入高效智慧生產模式等242,000千元。
農業技術團170,000千元，培訓農業技術人員作為農業師傅…
```
寫的是「做什麼」與金額，**原文完全沒有會計科目名**。這些多半出現在「一級科目標題句」之後，其加總等於該標題句的總額。

**建議做法：維持現狀（空白＋黃底標記待人工複核）**
理由：科目資訊不存在於文字中，任何規則都是臆測。已有的補救已經到位：
- 一級標題句裡的內嵌明細（`包括補(協)助政府機關(構)891,000千元、捐助國內團體511,000千元`）已被對帳抽取歸戶，金額層面不會漏。
- 這些列在主表以黃底 `.acct-filled` 標示，提醒人工確認。

**不建議的做法**
依標題句的內嵌比例回推分攤到各活動句——那是**捏造**分類，會讓錯誤看起來像正確答案。

---

## D. ~~`parse_detail.py` 欄位偵測漏掉整個基金~~〔已完成〕

已改為多形式比對（`px("前年度決算數", "前年度", "前")`），115 年度明細版
列數 656 → 712，`林務發展及造林基金` 56 列正常解析。

**但這支仍有一個弱點未解**：Python 沒有 JS 的 `_matchAccountLoose`，
遇到被欄寬截斷的名稱（如一級「會費、捐助、補助、分攤、」）會用嚴格比對
配到二級「會費」。若要拿 `parse_detail.py` 當第二意見，需先移植 JS 的
前綴補全邏輯。網頁工具不受影響。

<details><summary>原始問題描述</summary>

**現象**
`林務發展及造林基金` 的明細表頁（p172-174）被整批跳過，該基金 32 個二級科目在明細側完全消失。

**成因**
`_find_columns` 用**精確比對** `pos["前年度"]`，但該頁表頭是單一 cell「前年度決算數」（未被空白拆開）→ 找不到欄位錨點 → `if not cols: continue`。

**建議做法**
比照 JS 版 `_detailColumns` 的多形式比對：
```python
def px(*keys):
    for k in keys:
        if k in pos: return pos[k]
    return None
xF = px("前年度決算數", "前年度", "前")
```

**注意：影響範圍很小**
`parse_detail.py` **不被網頁工具使用**（明細版 tab 用的是 index.html 內的 JS），**也沒有掛進 MCP server**（`mcp_server.py` 只註冊 `parse_budget`）。僅在直接用 CLI 跑這支腳本時才有影響。

**驗收**
`parse_detail_pdf()` 應解析出 `林務發展及造林基金` 的二級科目（約 32 個）。

</details>

---

## E. 對帳 tab「僅一節出現」與剩餘不一致〔優先度：低〕

**現況（2026-07-28）**：不一致 5 筆、僅一節 7 筆、相符 168 筆。曾經的 86 筆「僅一節」多數已解決——成因是計畫層級不齊（無標號／單層計畫標題的基金整批落入「未分類計畫」），已修正。

**剩餘 5 筆不一致的成因（皆已定位，均不建議再用規則處理）**

| 差額 | 成因 |
|---|---|
| −120,824,000（農產品受進口損害救助） | 原文只有「政策目的＋金額」，完全無科目名（見 C 項），規則無解 |
| −68,440,000 / −22,500,000（農村再生） | 「委託○○辦理…等費用」不再沿用上一行科目後留下的誠實缺口 |
| −4,500,000（物料）／−50,000（車租） | 科目名僅 2 字，低於句中比對的長度門檻（≥3）；放寬會誤傷 |

另有一筆已知但**刻意不修**：`(二)社區農村再生計畫服務費用—編列其他專業服務費20,000千元` 把計畫名與科目黏在一行，且科目跨一級（7→2），被「同一級才採用」的防呆擋掉而沿用上一行。鬆綁該防呆的風險高於收益。

**「僅一節」的成因（兩種，性質不同）**
1. **正常的粒度差異**：明細表逐項列出每個有金額的二級科目；編列說明只描述有文字說明者。「僅明細版有」多屬此類，**不是錯誤**。
2. **口語詞對不到科目名**：如「工員1名工資」（正式員額薪資）、「銷售商品成本」（商品）、「專車接駁等車租」（交通及運輸設備租金）、「核發造林獎勵金」（補貼、獎勵…）。屬別名可解，但每條別名都可能誤傷其他基金，需照 `/rule-fix-loop` 逐條量化。

**建議做法**
目前 7 筆已在可人工消化的量級，「僅一節」區塊預設摺疊，暫不需再加工具。若日後換一份計畫命名差異大的預算書而數量回升，再考慮加「基金層級／計畫層級」切換鈕（基金層級會把計畫命名差異吸收掉）。

---

## F. 母基金彙總頁靠寫死基金名排除〔優先度：低〕

**現象**
`parseDetailDoc` 與 `buildMergedSources` 以 `!r.fund.includes('農業特別收入基金')` 濾掉母基金的彙總頁。
換一份其他機關的預算書／決算書，它自己的母基金彙總列不會被濾掉，會與各分基金重複計入。

**已排除的做法：改用結構性判斷**
試過「總額＝各分基金相加者即母基金」。114 年度自編決算實測**不成立**：

| | 一級科目合計（決算數） |
|---|---|
| 農業特別收入基金（母） | 59,693,080,702 |
| 六個分基金相加 | 56,800,956,789 |

差 28.9 億（5%），母基金頁不是分基金的單純加總（可能含未分列項目，或任一側解析不全，未查證）。
在沒有 ground truth 前改成結構規則，等於用臆測換掉一條已知會動的硬編碼。

**影響範圍（實測）**
- **預算書側空轉**：`examples/moa-fund-115.pdf`、`農業特別收入基金115年度預算案.pdf` 的母基金頁本來就解析不出任何列，這條件濾掉 0 列。
- **只有決算書真的濾掉東西**：`114年度自編決算.pdf` 濾掉 328 列。

**要處理時的前置作業**
先翻決算書 PDF 的母基金頁，確認那 328 列與分基金頁的關係（純彙總？含獨有項目？），
再決定規則。沒查清楚之前不要動。

---

## 重要認知：不要預設「明細表一定對」

用途明細表的科目與金額是一對一，**但解析器讀它一樣會出錯**。實例：農業天然災害
救助基金 p206 的名稱欄被欄寬截斷成「補貼、獎勵、慰問、照護」（缺「與救濟」），
比對迴圈由長到短掃描時被 74Y（其他補貼…）搶先命中，二級科目被誤判成三級，
該列在明細側彙總（只取二級）被整筆跳過，形成假的「僅編列說明有 61.6 億」。
說明版該筆與 PDF 原文完全相符，**錯的是被當作基準的那一側**。

所以看到差異時有**四種**可能，不是三種：

1. 明細表正確、編列說明漏寫或寫錯
2. 編列說明正確（已更新）、明細表沒跟上
3. 兩邊都對，只是粒度不同（不該比）
4. **任一側的解析器讀錯**（截斷名、欄位偵測、計畫歸屬…）

排查差異時，**先翻原始 PDF 確認兩邊的原文數字**，再判斷是誰的問題。

---

## 已完成事項（供追溯，勿重複處理）

| 項目 | 成因 | 修正 |
|---|---|---|
| 「千 元」金額漏抓 | PDF 換行在「千」「元」間插入空格 | 清理階段真正移除行內空白；regex 放寬為 `千\s*元` |
| 科目明細誤判為計畫 | 計畫 L1/L2 判斷未檢查「不含千元」 | 加 `!含千元` 條件；新增「中文數字＋頓號」條列前綴 |
| 帶括號計算式的科目行被吞 | calc-note 併回規則過貪 | 併回前加 gate：句首可比對到科目就不吞 |
| 對帳假差額（+30 億） | 「分述如下」小計與明細重複加總 | 排除小計標題引導詞；改為**逐區塊**取合計或加總明細 |
| 對帳說明版嚴重少算 | 二級金額只寫在一級標題句／接在「編列」後 | 掃描 l2 為空的列，抽出「科目名＋N千元」歸戶到二級 |
| 一級後接「－編列◯◯費」二三級空白 | 「編列」贅詞擋住往下比對 | 往下比對前去除 `編列/係` 前綴（修正 32 列） |
| 農村再生深層條列未歸類 | `(n)` 標號只准比對三級、字母標號不辨識 | `(n)` 改試 [3,2]；新增字母標號前綴 |
| 內嵌於標題句的科目明細未成列 | 只有對帳會拆、主表仍空白 | 解析器拆列，安全閥「子項加總==母額」 |
| 用途活動句重複計入 | 拆出的子列成為順序後處理的錨點 | 尋找錨點時跳過 derived 列 |
| 頁首被抽進句中切斷科目名 | 純中文行偵測上限僅 10 字，長基金名未被認出 | 上限放寬為 20，並從句中剝除（未匹配 2→0） |
| 明細側二級科目整筆消失 | 欄寬截斷名被由長到短的迴圈對到三級 | 先補全為「以此為前綴的最短科目」 |
| 明細側漏算只列三級的分類 | 只取「有二級無三級」的列，如「分攤」僅列三級 分擔其他費用1,860千元 即被跳過 | 有二級小計列用它，沒有才加總其三級列 |
| `parse_detail.py` 漏整個基金 | `_find_columns` 精確比對「前年度」，表頭為單一 cell 時找不到 | 改多形式比對（656→712 列） |
| 整支基金落入「未分類計畫」 | `planTop` 只認第一層（壹、），單層計畫的基金從「一、」起編 | 改取第一個有值的層級（相符 115→149） |
| 無標號計畫標題不被辨識 | 無標號規則要求同行含「基金」 | 放寬為「以計畫結尾＋無千元＋長度≤30＋無標號」（未分類 15→0 列） |
| 科目名寫在金額之後而歸不了戶 | `_embeddedAccount` 只對帶標號的條列行生效 | 無標號標題句也對「剝掉一級科目名的餘句」跑一次（孤兒句 56→45） |
| 短科目名攔截長科目名 | 句首比對依層級順序先中就停，二級「分攤」吃掉三級「分擔員工保險費」 | 各層級都比、取科目名最長者 |
| 「委託…辦理」誤沿用上一行科目 | 無科目名時一律沿用 | 句首為「委託」時留白成孤兒句（3 筆錯數字轉為誠實留白） |
| 「一級－二級同句」缺一級母列 | 該寫法只產一列，與「…包括：」寫法層級深度不一致 | 補一列同金額母列並標 `isSubtotal`（對帳不重複計入） |
